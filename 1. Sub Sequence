void subSeqence(int i, vector<int> s, vector<int> temp, vector<vector<int>> &ans){
    if(i>=s.size()){
        ans.push_back(temp);
        return;
    }

    temp.push_back(s[i]);
    subSeqence(i+1, s, temp, ans);
    temp.pop_back();
    subSeqence(i+1, s, temp, ans);
}
